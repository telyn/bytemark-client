package testutil

/*
func wevs() {
	response := nil
	object := nil
	url := ""
	endpoint := 0
	testName := ""

	rts := RequestTestSpec{
		Method:   "GET",
		Endpoint: endpoint,
		URL:      url,
		Response: response,
	}

	rts := RequestTestSpec{
		Method:          "PUT",
		Endpoint:        endpoint,
		URL:             url,
		ExpectedRequest: ``,
		Response:        response,
	}
	if !reflect.DeepEqual(response, object) {
		t.Errorf("%s didn't get expected object.\r\nExpected: %#v\r\nActual:   %#v", testName, response, object)
	}
}
*/
